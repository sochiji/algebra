package number;

/**
 * @see Fraction
 * @see Complex
 */
public interface Visible {

    //Scalar add(Scalar r);

    //Scalar sub(Scalar r);

    //Scalar mul(Scalar r);

    //Scalar div(Scalar r);

    //Scalar pow(int r);
    @Override
    String toString();
}